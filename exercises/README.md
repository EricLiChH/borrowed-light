# 所有权样章练习 🦀

先按照 [Rustlings 官方说明](https://github.com/rust-lang/rustlings)安装 Rustlings 6.5.0。

在本目录运行：

```sh
rustlings
```

练习按 `move → borrow → mutable borrow` 排序。输入 `h` 查看方向提示，输入 `l` 查看进度。更细的三层提示位于 mdBook 的“分层提示”页面。
